# Point this at your running HMS.Api backend.
# Check the console output when you `dotnet run` the backend for the exact port —
# it's usually printed as "Now listening on: https://localhost:7xxx".
VITE_API_BASE_URL=https://localhost:7000/api
